#pragma once
#include "Utilize.h"
using namespace std;


static tokenType reservedLookup(char *s);
bool isLetter(char c);
bool isDigit(char c);
static tokenType reservedLookup(char *s);


void getToken(string ss);


